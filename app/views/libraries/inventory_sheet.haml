%h2="Inventory Sheet - #{@library.street}"

%table{:class => 'table'}
  %thead
    %th In 
    %th Out
    %th Title
    %th Added
    %tbody
    -@books.each do |book|
      %tr
        %td &#9634;
        %td &#9634;
        %td=book.title
        %td=book.created_at.strftime("%B %d, %Y")
